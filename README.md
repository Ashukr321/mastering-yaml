```bash
mastering-yaml/
├── 01-syntax-fundamentals/
│   ├── basic-types.yaml      # Strings, numbers, booleans
│   ├── collections.yaml      # Lists (arrays) and Maps (objects)
│   └── advanced-syntax.yaml  # Multi-line strings, Anchors (&), and Aliases (*)
├── 02-real-world-configs/
│   ├── docker-compose.yml    # Example for a React + Node + MongoDB stack
│   ├── github-actions.yml    # CI/CD pipeline for linting and testing
│   └── kubernetes-pod.yaml   # Basic K8s deployment example
├── 03-practice-challenges/   # Empty files with "To-Do" comments
└── README.md                 # Documentation and YAML "Gotchas"
```
